[project]
name = "onboarding-mcp"
version = "0.1.0"
description = "MCP server with Google search tool"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.14.5",
    "httpx>=0.27.0",
    "lib-logger",
]

[tool.uv.sources]
lib-logger = { workspace = true }
